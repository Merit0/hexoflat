/**
 * Mirrors apps/web's router route names. Duplicated (not imported) so the
 * engine stays framework/app-agnostic — see docs/MIGRATION-PLAN.md Phase 2.
 */
export type RouteName = 'battle' | 'world';
